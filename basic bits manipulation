___________________________________________________________________________________________________________________________________________
# unique element in an array ,if all the elements are present twice except one

int xor=0;
for(int i=0;i<nums.size();i++) xor = xor^nums[i];
cout<<xor<<endl;

____________________________________________________________________________________________________________________________________________
# swapping of two numbers a,b without using third variable

a = a^b; 
b = a^b;
a = a^b;

there is an arithematic way to swapp two numbers without using third variable
